// preload.js — se ejecuta en el renderer antes del HTML, con Node deshabilitado.
// Aquí puedes usar contextBridge.exposeInMainWorld() si en el futuro necesitas
// comunicar el renderer con el proceso principal (IPC).
